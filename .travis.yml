language: cpp

compiler:
  - gcc
  - clang
python:
  - "2.7"
  - "3.6"

before_install:
  - sudo apt-get update -qq

install:
  - sudo apt-get install -qq default-jre
  - pip install --user subprocess32
  - make download

before_script:
  - make init

script:
  - make bin/sbu
  - make test
