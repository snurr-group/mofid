#!/bin/bash
#PBS -N test-names
#PBS -o pbs.out
#PBS -e pbs.err
#PBS -r n
#PBS -V
#PBS -mba

cd $PBS_O_WORKDIR
echo $PBS_JOBID > jobid

OUTPUT_DIR="Output/"
mkdir -p "$OUTPUT_DIR"
bash Scripts/extract_folder.sh Resources/TestCIFs/ "$OUTPUT_DIR"

